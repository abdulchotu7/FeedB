<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feed</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="w-full min-h-screen bg-zinc-800 text-white">
    <div class="container mx-auto p-4 w-3/4 m-3">
        <% allposts.forEach(post => { %>
            <div class="bg-white border-2 w-full rounded-lg shadow-md p-4 mb-4">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="font-semibold text-blue-700"><%= post.username.username %></h3>
                    <h5 class="text-gray-500 text-sm"><%= post.createdAt.toDateString() %></h5>
                </div>  
                <div class="relative mb-4">
                    <p class="w-full text-black font-bold h-32 p-2 border-2 border-gray-200 rounded-lg resize-none focus:outline-none focus:ring-2 focus:ring-blue-500"><%= post.content %></p>
                </div>
                <div class="flex items-center">
                    <a class="bg-yellow-400 text-black rounded-md p-2 w-20 text-center mr-4" href="/like/<%= post._id %>">Like</a>
                    <span class="text-gray-500"><%= post.likes.length %> Likes</span>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>
